on:
    push:
      branches:
        - main
    workflow_dispatch:

jobs:
    terraform-apply:
        uses: ./.github/workflows/terraform-apply.yml
        permissions:
            contents: read
        secrets: inherit